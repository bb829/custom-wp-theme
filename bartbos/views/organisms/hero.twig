<section class="section {{fields.font_color ? fields.font_color}} section--first overflow-hidden" style="background: {{fields.background_color}}">

	<div class="hero d-flex align-items-center justify-content-center fade-in delay-2 pt-3 pt-md-0">

		{% if fields.background_video %}

			<div class="textImage__videoOverlay position-absolute w-100 h-100"></div>
			<video class="w-100 h-100 position-absolute top-0 end-0 bottom-0 start-0 cover object-center grey" src="{{fields.background_video}}" autoplay muted playsinline nocontrols loop></video>

		{% endif %}

		<div class="container">
			<div class="row">
				<div class="col-12 {{fields.animation ? 'col-md-6' }} {{fields.image ? 'col-md-6' }} d-flex flex-column align-items-start justify-content-center z-4" style="perspective:1000px;">
					<div class="swing-in-top-fwd delay-4">

						{% if fields.title %}

							<h1 class="hero__title">{{fields.title}}</h1>

						{% endif %}

						{% if fields.content %}

							<div class="hero__content">
								{{fields.content}}
							</div>

						{% endif %}

					</div>

					{# <pre>{{dump(fields)}}</pre> #}

					{% if fields.buttons.button_left or fields.buttons.button_right %}

						<div class="d-flex mt-4 gap-3 fade-in delay-5">

							{% if fields.buttons.button_left.url and fields.buttons.button_left.title %}

								{% include 'atoms/button.twig' with {
										'url': fields.buttons.button_left.url,
										'title': fields.buttons.button_left.title,
										'target': fields.button_left.target,
										'style': fields.buttons.button_left_style
									}
								%}

							{% endif %}

							{% if fields.buttons.button_right.url and fields.buttons.button_right.title %}

								{% include 'atoms/button.twig' with {
										'url': fields.buttons.button_right.url,
										'title': fields.buttons.button_right.title,
										'target': fields.buttons.button_right.target,
										'style': fields.buttons.button_right_style
									}
								%}

							{% endif %}

						</div>

					{% endif %}

				</div>

				{% if fields.animation %}

					{% include 'molecules/heroAnimation.twig' %}

				{% endif %}

				{% if not fields.animation and fields.image %}

					<div class="col-12 col-md-6 d-flex align-items-center justify-content-center scale-in-center delay-4 z-4">
						<img src="{{fields.image.url}}" class="textImage__image cover"/>
					</div>

				{% endif %}

			</div>
		</div>
	</div>
</section>
